.cloudflare.env
node_modules/
.env
.env.local
# Vercel CLI link/pull output (repo-root link for the monorepo deploy of web/)
.vercel

# Test-script debug output — regenerated by npm run test:*, not source
extraction_output.json
*.log
pipeline_report.md
pipeline_result.json
sample_report.md
# Per-run manifest edit files fed to the stage driver's confirm command
edits_run_*.json

# Client project source PDFs — real project material, not tool source, don't belong in git history
Docs/*.pdf

.vercel
